doctype html
html
  head= partial 'layouts/head'
  body class=page_classes
    .grid
      = partial 'layouts/header'
      .page
        .page__sidebar
          .sidebar
            = partial current_page.data.sidebar
            = render_toc(current_page)
        .page__content
          .content
            = yield
      = partial 'layouts/footer'
